@model IEnumerable<TEMPLATE.Models.association>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_association)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.colline.NOM_colline)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NOM_association)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TEL_association)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DATE_association)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_association)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.colline.NOM_colline)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NOM_association)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TEL_association)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DATE_association)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_association ,id2=item.ID_colline }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
